# 현대자동차 인포테인먼트개발 인턴 — 자소서 초안 v2
> 작성일: 2026-05-05 | 코드 직접 검토 반영 | 예상 약 740자

---

## ✏️ 본문

LLM 기반 멀티에이전트 시스템을 처음부터 혼자 설계하고 구현했습니다.

숭실대학교 컴퓨터공학부 재학 중, GraphRAG 기반 금융 분석 멀티에이전트 시스템을 개발했습니다. LangGraph로 의도 분류·컨텍스트 검색·재무 분석·리스크 감사·반성·비평·오케스트레이터 등 8개 전문화 에이전트를 오케스트레이션하고, FastAPI RESTful API와 마이크로서비스 패턴으로 서비스 레이어를 구성했습니다. Neo4j와 Qdrant를 결합한 하이브리드 검색으로 멀티홉 컨텍스트 정확도를 높였습니다.

핵심 과제는 '실시간성'이었습니다. SSE(Server-Sent Events)와 asyncio Queue 기반 ping 에미터를 직접 설계해 멀티에이전트 처리 결과를 클라이언트에 실시간 스트리밍했습니다. 노드 처리 중 네트워크가 유휴 상태가 되면 프록시가 연결을 조용히 끊는 문제를, 15초 간격 keepalive ping으로 해결했습니다. 노드 응답이 30초를 초과하면 TimeoutError를 catch해 fallback 이벤트를 송출하고 graceful하게 종료했으며, 사용자 중단 요청은 interrupt 엔드포인트와 cancel 폴링으로 처리해 finally 블록에서 태스크를 빠짐없이 정리했습니다.

Prometheus 메트릭과 OpenTelemetry 분산 추적으로 노드별 지연·오류·트래픽을 가시화하고, JWT 인증·가드레일·감사 로그로 보안 계층도 직접 구성했습니다.

커넥티드 카 백엔드가 요구하는 실시간 알림 시스템 고도화와 지능형 모니터링은 이 경험에서 직접 파생됩니다. 컴퓨터공학 전공과 금융학부 부전공의 교차점에서 기술과 도메인을 함께 보는 시각을 갖고 있습니다. 절대적인 정답보다 현재 최선의 선택을 실행하고 검증하며 수정하는 방식으로, 팀과 함께 더 나은 시스템을 만들어가겠습니다.

📎 github.com/JS0208

---

## 📊 v1 → v2 변경 사항 (코드 검토 반영)

| 항목 | v1 (수정 전) | v2 (수정 후) | 근거 |
|---|---|---|---|
| 타임아웃 설명 | "30초 타임아웃으로 스트림 종료" | "노드 응답 30초 초과 시 TimeoutError catch → fallback 이벤트 송출 후 graceful 종료" | `asyncio.wait_for(anext(stream), timeout=30)` |
| cancel 처리 설명 | "asyncio 태스크 누수를 cancel 인터럽트 핸들러로 차단" | "interrupt 엔드포인트 + cancel 폴링 → finally 블록에서 태스크 정리" | `/interrupt/{thread_id}` + `is_cancel_requested()` 폴링 구조 |
| ping 간격 | 명시 없음 | "15초 간격 keepalive ping" | `_ping_emitter(interval_s=15.0)` |
| 보안 계층 | "JWT, 감사 로그" | "JWT 인증·가드레일·감사 로그" | `GuardrailVerdict` + `classify_input()` + `audit_event()` |
| GitHub ID | [GitHub_ID] 플레이스홀더 | `github.com/JS0208` | 사용자 확인 |

---

## 📊 자가 점검

| 항목 | 결과 |
|---|---|
| 예상 자수 | 약 740자 (목표 700~850) ✅ |
| 오프닝 방식 | ✅ 행위로 시작 |
| 핵심 메시지 | ✅ "커넥티드 카 백엔드 기술을 이미 구현" |
| JD 키워드 | ✅ 실시간 알림 시스템, 지능형 모니터링, RESTful API, 마이크로서비스 |
| Gleo AI 암시 | ✅ LLM 오케스트레이션 (직접 언급 X) |
| 기술 사실 정확도 | ✅ 코드 직접 확인 완료 |
| 스펙 나열 | ✅ 없음 |
| GitHub 링크 | ✅ github.com/JS0208 (README 정비 필요) |
| 사고방식 표현 | ✅ "현재 최선을 실행하고 검증하며 수정" |
| 면접 유발 문장 | ✅ SSE + cancel 폴링 구조 → "어떻게 설계했나요?" 유발 |

---

## 📌 남은 작업

1. **GitHub README 정비** — 링크 첨부 전 필수
   - 프로젝트 한 줄 요약 (한·영)
   - 기술 스택 배지
   - 아키텍처 플로우 이미지
   - .env.example 민감 키 점검
2. **PDF 실제 제작** — 양식_전략.md 기반 (Python HTML→PDF)
3. **최종 체크리스트** — 자소서_전략.md § 9 기준
